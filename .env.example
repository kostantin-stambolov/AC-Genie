DATABASE_URL="file:./prisma/dev.db"
SESSION_SECRET="your-secret-at-least-32-chars"
ADMIN_EMAIL="admin@example.com"
ADMIN_PIN="1234"

# Optional: for essay dictation and transcription (OpenAI Whisper)
# OPENAI_API_KEY=sk-...

# Optional: secret for /login/reset to unblock "Too many attempts" (production only)
# RATE_LIMIT_RESET_SECRET=your-rescue-secret
